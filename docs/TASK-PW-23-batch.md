# TASK-PW-23 Harness 写权限落地批说明

- 日期：2026-08-06
- 状态：**完成**——五刀全部交付并验收（2026-08-07）：PW-23 审计地基（113/113）→ PW-24 人发话写工具（120/120+冒烟六步）→ PW-25 铸币级+冲正键、PW-26 AI 自主档（并行，138/138+双冒烟：挑卡/撤销/两跳合一结账/作废/查账；自主抓取全链路两条视频各 100 评论、镜像自动、分拣自动）→ PW-27 指认对齐与监督收口（144/144 + audit:harness 真实库 0 违规 + 菜号牌 ego 视觉验收）。《Harness 写权限边界规格》v1 全链路落地：AI 工具全开、决策权在人、店规 A 运行中、硬闸门 B 留作升级路径
- 依据：《Harness 写权限边界规格》v1（`docs/SPEC-harness-write-boundary.md`，2026-08-06 用户验收）；wayfinder 地图 `docs/wayfinder/harness-tooling/`（R01 写动作盘点、T01 分档、T02 指认、T03 审计与撤销）；`docs/REQUIREMENT-DOMAINS.md` v0.4（「永远 deny」条款废止，权限模型升级为「发动权在人」）

## 批次范围（5 刀）

顺序依据规格 §8：先修小票机（审计），再发钥匙（写工具），铸币级单独一刀，自主档随后，指认与监督收口。

| TASK | 交付 | 主需求域 | 依赖 |
|---|---|---|---|
| PW-23 审计地基扩容 | pw_runs schema 扩（actor=ai 执行类放开 + 指令引用字段）+ 审计盲区全补 + 迁移 + 测试 | 判断沉淀与复用（沿 PW-11 事件日志归属） | 无 |
| PW-24 人发话写工具进表（非铸币批） | exec 档工具：草稿确认/驳回、直接建注、押注编辑、数据文档冻结、手动补筛、声音录入、挂载/摘除产出物 + 查账只读工具 + 店规提示词 v1 | 判断沉淀与复用 | PW-23 |
| PW-25 铸币级执行工具与冲正键 | 结账（两跳合一跳）+ 挑卡/否卡/全否重筛 + 撤销挑卡通路 + 结账作废通路 | 判断沉淀与复用 | PW-23、PW-24 |
| PW-26 AI 自主档落地 | fetch_corpus ask→自主直抓（行为限速）+ 金子镜像自动 + 声音分拣自动 | 实践与数据回收 | PW-23、PW-24 |
| PW-27 指认对齐与监督收口 | 菜号牌对齐（装配+前端标签）+ 擅自发动自动断言/冒烟 + 规格对照回勾 | 内容生产 | PW-24/25/26 |

执行顺序：PW-23 → PW-24 →（PW-25、PW-26 可并行）→ PW-27 收口。

## 逐刀四字段

### PW-23 审计地基扩容

- 主需求域：判断沉淀与复用
- 业务接口：为各域全部写动作提供记账（跨域底座；本刀只扩机制，不改业务语义）
- 数据真值源：`pw_runs`（schema 扩容：actor=ai 不再强制 kind=ai_draft，新增执行类 kind/event_type；新增指令引用——人指令原文 + 来源消息 id）；迁移照 migratePwRunsCheck 先例（旧库重建）；voice 三写 / 语料四写 / 连接两写 / 金子镜像 / 协作消息全部接入 recordPwEvent
- 质量约束与验收终态：账本只增不改（append-only 不破）；每个既有写动作落账断言（含原盲区逐点）；旧库迁移测试；PW-11/15/18/19 既有测试全绿；`PATH="$HOME/.local/node/bin:$PATH" npm run verify` 全绿

### PW-24 人发话写工具进表（非铸币批）

- 主需求域：判断沉淀与复用
- 业务接口：无（域内工具表扩展；供协作台两种对话消费）
- 数据真值源：无新表；`pw-collab-tools.ts` 工具表扩容——CollabToolPolicy 新增 `exec` 档（语义：写动作，仅在用户明确指令后调用，工具描述写死）；进表：押注草稿确认（可带 edits）/驳回、直接建押注、押注编辑（标题/指标/结账日）、数据文档冻结、手动补筛、声音录入、挂载/摘除产出物；新增 allow 只读「查账」工具（对话内回答"你最近代办了什么"）；deny 名单同步缩到「AI 默认不开」集（删押注/改判决/候选卡常规回退/水位重置/消息删除/镜像强刷/风控清空）；店规提示词 v1 进 system prompt（写工具纪律、指认四规、行为限速、逐字引用延续）
- 质量约束与验收终态：每个 exec 工具落账必带指令引用（断言）；deny 新集断言延续；店规 prompt 文本断言；真实模型对话冒烟（人发话→执行+落账；未发话→不碰写工具）；verify 全绿

### PW-25 铸币级执行工具与冲正键

- 主需求域：判断沉淀与复用
- 业务接口：登记实践执行（挑卡建押注沿用既有接口语义）
- 数据真值源：无新表；exec 档进表：结账（两跳合一跳：按已固化草案一步 settle，草案内容即结账内容）、挑卡/改挑/否卡（消费 pw-content-bets 既有函数）、全否重筛（复合：逐张否+触发补筛）；新冲正键通路（exec）：**撤销挑卡**（生成的内容押注作废 + 候选卡退回 pending，同一事务）与**结账作废**（settled→void、判决同废，可重结）；候选卡常规回退仍 deny（冲正键是唯一回退口）
- 质量约束与验收终态：状态机断言（冲正前后行态、唯一索引不破）；冲正键仅限撤销语义（工具描述与测试双重钉死）；落账带指令引用；真实模型冒烟（"就这张"→挑卡+落账+可查账；"撤销"→冲正）；verify 全绿

### PW-26 AI 自主档落地

- 主需求域：实践与数据回收
- 业务接口：提供语料与回流数据（语料抓取改为 AI 自主直抓，供给关系不变）
- 数据真值源：`pw_corpus_docs`（fetch_corpus 由 ask 改自主：不再落 proposed，直接进抓取执行）；抓取执行路径=对话内 AI 触发（进程内复用抓取逻辑或子进程调用既有脚本，执行规格定）；**行为限速**：读风控/限速信号自行放慢并反馈、撞墙 needs_human 交人（写进店规与工具描述）；金子镜像自动（结账/确认后自动同步，UI 按钮保留为后路）；声音分拣自动（录入后自动 classify）
- 质量约束与验收终态：事前批准链（proposed→approve）废除，既有 propose/approve 路由保留为人工后路；抓取/镜像/分拣全落账（消费 PW-23）；限速纪律断言；真实抓取冒烟（小量）；verify 全绿

### PW-27 指认对齐与监督收口

- 主需求域：内容生产
- 业务接口：提供选题候选（菜号牌对齐：装配注入短标签与页面标签同源）
- 数据真值源：无新表；装配层注入对象带短标签（证据 N/注 N/草稿 A/BV 号），前端卡片标签核对同源；擅自发动自动断言（扫描 pw_runs：执行类 ai 账缺指令引用即失败）+ 冒烟脚本；规格对照回勾表（SPEC 八节 → 实现落点逐节核对）
- 质量约束与验收终态：断言与冒烟可重复跑；指认冒烟（"就这张"锚定、歧义反问）；规格八节逐节有落点证据；verify 全绿

## 防冲突约定（沿 PW-17 批先例）

1. 每个 TASK 只新建/修改自己名下的文件，集合互不相交；路由挂载与测试登记留集成阶段（PW-27 或各刀执行规格明确处）。
2. 跨模块引用只存 id 字符串；测试各自内存库自包含。
3. 每任务独立验收：`node --test src/pw-<name>.test.ts` 全绿 + selfcheck 不退化；前端涉及时 `PATH="$HOME/.local/node/bin:$PATH" npm run verify`。
4. 不 commit；执行分工沿用：新窗口 DeepSeek 子代理按执行规格交付 → 主代理亲自验收（verify + 真实模型冒烟）。

## 不在本批（明确排除）

- 硬闸门 B 的实现（留作店规失效后的升级路径，规格 §6）
- 审计 UI 页面（对话内查账代替，真需要再补）
- 声音→草稿提升链（留 P2 观众声音）
- 素材制作 run、筛子 v2（另起 effort）
- 大盘屏任何改动
