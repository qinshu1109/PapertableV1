# TASK-023 Mermaid 节点文字完整展示

- 状态：in_progress（完成后等待用户验收）
- 主需求域：可信资料求证
- 业务接口：返回可信回答（可信资料求证 → 非线性知识探索）
- 数据真值源：`pt_runs.answer` 中既有 Markdown / Mermaid 内容；本任务不修改持久化数据
- 质量约束与验收终态：图形源码、引用和缩放行为不变；Mermaid 节点文字不得裁掉末字；`npm run verify` 通过

## 目标

修复 Mermaid 图块被代码块 `<pre>` 包裹后继承等宽字体，导致实际文字宽于 Mermaid 布局宽度、节点末字被裁掉的问题。

## 验收

1. Mermaid 图块不再继承代码块的等宽字体。
2. 长中文节点的最后一个字完整显示。
3. 普通代码块、查看源码、复制和放大查看保持可用。
