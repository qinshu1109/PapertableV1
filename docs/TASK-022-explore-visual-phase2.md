# TASK-022 关系图视觉对标第二阶段（Explore 对齐）

- 状态：in_progress（完成后保持，等待监督者验收）
- 分支：task/TASK-022-explore-visual-phase2
- 依据：research/graph-visual-compare-2026-08-02/REPORT.md（Ego Lite 现场实测）+ 用户拍板 AAC（按执行者建议细化）
- 主需求域：非线性知识探索
- 业务接口：无（域内视觉与交互调整）
- 数据真值源：`pt_cards`、`pt_edges` 及既有 `pathToRoot()` 关系语义；本任务不修改持久化数据
- 质量约束与验收终态：三种关系语义与当前路径计算不变；拖拽不误触点击；完成状态以本卡验收标准 1–7 为准

## 目标画面（用户已确认）

| 选择卡 | 决定 | 细化边界 |
|---|---|---|
| 卡1 路径节点渲染 | A（采用 Explore 黑实心） | **视觉 A / 语义 B**：路径节点改黑色实心；染色语义保持 Papertable 现状——深挖/发散/改道三类关系的当前路径都染；根节点珊瑚环优先（路径上根仍带珊瑚环，不被黑色吃掉） |
| 卡2 卡片区构图 | A（宽度采用 Explore） | **宽度 A / 透明度 B**：主卡片 880px → 1207px（对齐 Explore 实测）；卡体保持不透明，不引入 Explore 的透字叠影 |
| 卡3 关系入口按钮 | C（混合） | 保留图标+中文短标签（改道/深挖/发散），去掉方向文案（向左分岔/沿路径向下/向右展开），按钮瘦身 |
| 卡4 图栏拖拽平移 | 用户追加（会话内） | 对齐 Explore：图栏可左键拖拽平移（cursor grab/grabbing）；小树下 SVG 最小画布 720×1150 保证随时可拖；拖拽与点击/展开收起互不干扰；手动滚动/拖拽 3 秒平滑归位为既有行为，本任务不改 |
| 卡5 悬停提示条走位 | 用户报障（会话内） | 提示条原挂在滚动容器内，横向滚动/拖拽后跟随内容左移、被主卡片裁掉；改为挂在 `nav.graph` 上钉在栏底（bottom: 8px），不随画布滚动 |

## 明确不做

- 不改图布局常量（225px 图栏、36/80 间距、r14/16.8 已一致）；
- 不改三类关系的 pathToRoot 语义；
- 不引入卡片透明/叠影；
- 不动 Explore 一键建卡 vs Papertable 对话框建卡的交互差异（本阶段只视觉）；
- 不提交、不推送（按治理等验收）。

## 验收标准

1. 图栏当前路径上的非当前节点渲染为黑色实心填充 + 深色 3px 描边（对齐 Explore tree-path：fill #000 / stroke #3C2D28 3px）；当前节点样式不变；根节点珊瑚环在任何状态下保留。
2. 深挖、发散、改道三种当前卡的路径节点染色均生效（三类都染）。
3. 主卡片最大宽度 1207px（窗口 1694×926 时实测卡片宽 ≈1207px）；卡体背景不透明度不变。
4. 卡片底部关系按钮只显示图标+改道/深挖/发散，无「向左分岔/沿路径向下/向右展开」文案；按钮视觉瘦身。
5. `cd frontend && npm run build` 通过；浏览器实拍（Ego Lite，测试项目「图谱对标A」）核对 1–4 并留截图。
6. 图栏可用鼠标左键拖拽平移（拖拽前后各留一帧）；拖拽后的 click 不误触发展开/收起或跳卡。
7. DESIGN_NOTES.md 同步更新。
