# 简报 22：第一期·浏览器+接口验收（codex 执行，只验不修）

- 主控：kimi（w1:p1）；执行：codex（w1:pJ）；**修复回路（2026-08-14 用户改令）：测试发现问题由 codex 直接投递到 dsh-cc 通道 w1:pK 修**（`herdr agent prompt w1:pK "..."`，附问题简报路径），前端问题仍留主控 kimi 亲自修；修完 codex 复测。
- 日期：2026-08-14
- 依据：简报 21（后端三项）+ 主控前端（押注台 blotter/异常队列、大盘双账）。后端完工报告 `agent-bridge/out/21-backend-done.md`。

## 约束

- **只验不修**：不改任何代码、不改验收标准；守门文件一律不碰。
- **生产真库只读**：`127.0.0.1:4317` 上不许激活/结账任何真实押注。激活写操作要验就去隔离实例：`PAPERTABLE_DATA_DIR=/tmp/pw-acc-data node src/main.ts`（端口换 4399），验完即弃。
- 截图存 `agent-bridge/qa-evidence/`（前缀 22-）。

## 验收清单

1. **押注台默认首页=承诺总账**（生产 4317）：
   - 打开押注台默认不是单卡，是总账表：6 笔在途押注（真实数据：3 笔「到期缺数据」、3 笔「在途」）；
   - 异常队列在顶部，3 笔到期缺数据的押注在列，带「补数据」按钮；
   - 表行字段齐：编号/标题/看结果日/把握/验证指标/数据/状态；
   - 点行进单卡驾驶舱（原布局无回归：押注卡、分支卡、开干位、判决簿、闭环带都在）；
   - 「← 返回总账」能回到总账；
   - 异常队列「补数据」按钮能进对应押注的录入数据弹层。
2. **大盘双账**：目标横幅下方出现「双账 · 判断成绩与运行健康」区块；认识论账显示有效结账 1；运行账七项有值或如实为 —（不许出现 NaN/undefined 字样）。
3. **接口复核**：`GET /api/pw/bets/ledger` 与 `GET /api/pw/health-accounts` 返回结构与简报 21 契约一致（字段名、枚举值）。
4. **闸门（隔离实例）**：预检 fail 的草稿激活返回 409+失败清单、零写入；全 pass 草稿激活 200 且状态变 pending；仅 needs_human（手动录入源）能激活且响应带提示；preflight 本身幂等零写（调两次库无变化）。
5. **红线**：押注台/大盘其余区域无布局崩坏（视口 1808），无横向滚动条；浏览器控制台无报错。

## 回报

写 `agent-bridge/out/22-acceptance.md`：逐项通过/失败 + 证据（截图路径、curl 摘录）。失败项按「前端问题」/「后端问题」分类——主控按分类路由（前端 kimi 亲自修，后端派 worker）。
