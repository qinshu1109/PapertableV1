# 17 · 提案契约 + 运维屏 + 提案检查器：浏览器验收（codex）

先读 `agent-bridge/GUARDRAILS.md`，红线默认生效。

## 背景

简报 16 三件已落地：后端提案契约（claude，381/381 绿）、前端运维屏+提案收件箱+「询问此提案」
（kimi，tsc/build/headless 自检过）。后端已重启。真库里有一条 kimi 提交的联调自测提案
（lane=ops，标题「联调自测提案（可否决）」），专供你验收时行使否决权。

## 验收清单

入口：http://127.0.0.1:4317/ → 镇纸。

1. **运维屏**：导航出现「运维」（协作台后）。屏内：对账明细条（待你判断 5，含 提案 1）、
   待验收提案区（联调自测提案卡：kimi · report · 检查×1）、最近动态表。卡片点开弹窗：
   支持证据「curl 联通」、自动检查 selftest: pass、payload 可展开、接受/退修/否决三键。
2. **人行使否决**：对该自测提案点「否决」，填理由「验收流程自测」，确认。预期：提案从
   待验收消失；对账条 待你判断 5→4；`curl /api/pw/proposals?lane=ops` 该条 status=rejected
   且 reviewNote 留存。这是关键端到端点：人审动作真实落账。
3. **协作台内容车道**：协作台顶部 rail 下方**不出现**提案区（当前无 content 提案）——
   不出现即正确。可选：curl 提一条 lane=content 的测试提案，协作台应出现「待验收提案」区，
   点「询问此提案」应打开对话抽屉并自动发出一条关于该提案的问题；验完否决掉它。
4. **模式条回归**：协作台模式条四段正常、悬停文案正常；rail 无横向滚动条（沿用 15R 口径）。
5. **红线**：UI 上没有「创建提案」入口（提交是 agent 通道）；除提案的审按钮外两屏无新写按钮。

## 纪律

只验收不改代码。问题回路：后端 → 简报发 claude（w1:p9）；前端 → 注明「回 kimi」。
不 commit 不 push。完成后回报逐条结论 + 截图路径。
